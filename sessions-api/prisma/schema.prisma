generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Session {
  id       String @id @unique
  quality  Int    // probably won't be stored in database -> calculated by commit feedback
  duration Int
}
